Compile with:

g++ -O2 -fopenmp main.cpp

In main.cpp, check that the file path for the input is correct.
This will outout a file named density.txt which contains the new computed values.

To plot the results, use:

gnuplot -p plot.gnu